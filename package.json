{
  "name": "careero",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}